# Configuration

ShiggyBot's configuration system is deliberately simple — no configuration framework, just a `BotConfig` class that reads from three sources.

## Pages

- [Configuration Keys](/configuration/keys) — reference of every supported key
- [Config Sources](/configuration/sources) — loading order, file formats, and gotchas
- [Discord Permissions](/configuration/permissions) — required permission list
